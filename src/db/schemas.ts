import { customerProductPricesTable, customersTable } from './schemas/customer';
import { marketProductsTable, productsTable } from './schemas/product';

export const allSchemas = {
  customersTable,
  productsTable,
  marketProductsTable,
  customerProductPricesTable,
  // Always add new tables here..
};
